apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flask-color-changer.fullname" . }}-test-connection"
  labels:
    {{- include "flask-color-changer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flask-color-changer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
